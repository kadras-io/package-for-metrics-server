name: Release

on:
  - workflow_dispatch

jobs:
  release:
    name: Release
    permissions:
      contents: write
      id-token: write
      packages: write
    uses: arktonix/cloud-native-utils/.github/workflows/package-release.yml@main
    with:
      package-name-slug: metrics-server
      package-name-display: Metrics Server
      registry-server: ghcr.io
      registry-username: ${{ github.actor }}
      image: ${{ github.repository }}
      version: 0.6.1+kadras.1
    secrets:
      pull-request-token: ${{ secrets.GH_ORG_PAT }}
